// Elementle — Supabase configuration
//
// To enable the shared daily leaderboard (see all players' scores for today),
// create a free project at https://supabase.com and follow the setup steps in
// README.md, then fill in your project URL and anon/public key below.
//
// Leaving these empty disables the daily leaderboard; all other features work
// without a backend (per-user local stats, friend code import/export, etc.).

const SUPABASE_URL      = ''; // e.g. 'https://abcdefghijkl.supabase.co'
const SUPABASE_ANON_KEY = ''; // Your project's anon / public key
